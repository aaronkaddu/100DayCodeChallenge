# Variables are containers for storing data values
# We use th len() function to count the characters in a string
print(len(input("Enter your name: ")))

# Another Version
username = input("Enter your name: ")
length = len(username)
print(length)